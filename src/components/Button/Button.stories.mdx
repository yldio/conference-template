import {Meta, Story, Preview, Props, Description} from '@storybook/addon-docs/blocks'
import {text, addDecorator} from '@storybook/addon-knobs/react'
import { action } from '@storybook/addon-actions';
import Button, {Variants} from './'
import {ThemeProvider} from 'theme-ui'
import defaultTheme from '../../themes/default'

<Meta 
  title="Atoms|Button" 
  component={Button} 
  decorators={[(storyFn) => <ThemeProvider theme={defaultTheme}>{storyFn()}</ThemeProvider>]}
/>

# Button

The button abstract a HTML `<button>` element, with styles according to the
theme being used.  It's ideal to be used in actions, like forms, or part of a
UI interaction.

> Don't use the `Button` component to represent links to other pages.

<Preview withToolbar>
  <Story name="default">
      <Button onClick={action('onClick')}>{text('children', 'Hello World')}</Button>
  </Story>
</Preview>

<Props of={Button} />

## Variants

<Preview withToolbar> 
  <Story name="Dark Variant">
    <Button variant={Variants.Dark}>{text('children', 'Hello World')}</Button>
  </Story>
  <Story name="Light Variant"> 
    <Button variant={Variants.Light}>{text('children', 'Hello World')}</Button>
  </Story>
</Preview>


